# Phase 2b: Skip all ALC1220 fixups, use generic HDA parser
# This lets the parser see node 0x1b's full 5-connection list
# and potentially auto-assign DAC 0x03 as an independent output.
#
# Revert: delete /etc/modprobe.d/test-generic.conf and reboot
options snd-hda-codec-realtek model=generic
